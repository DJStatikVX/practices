var hungerLevel = 10;

if (hungerLevel > 7) { // we can also use =>, <=, !==, ===, etc. for comparing a variable's value to a set condition
  console.log('Time to eat!');
} else {
  console.log('Let\'s eat later.');
}
